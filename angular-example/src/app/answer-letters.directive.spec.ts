import { AnswerLettersDirective } from './answer-letters.directive';

describe('AnswerLettersDirective', () => {
  it('should create an instance', () => {
    const directive = new AnswerLettersDirective();
    expect(directive).toBeTruthy();
  });
});
