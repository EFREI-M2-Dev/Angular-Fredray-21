import { DateAnswerPipe } from './date-answer.pipe';

describe('DateAnswerPipe', () => {
  it('create an instance', () => {
    const pipe = new DateAnswerPipe();
    expect(pipe).toBeTruthy();
  });
});
